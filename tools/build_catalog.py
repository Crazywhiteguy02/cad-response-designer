"""Rebuild data/unit_catalog.csv from a `pdftotext -layout` extraction of the
user-provided All unit definitions PDF.

This helper documents the import approach used for v0.4. The generated CSV is
already included in the project, so normal users do not need to run this script.
"""
from pathlib import Path
print("The v0.4 package already includes data/unit_catalog.csv. Re-import is a developer task.")
